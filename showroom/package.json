{
  "name": "chassis-showroom",
  "version": "1.0.0",
  "description": "",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-prod": "npm link @chassis/core && node build.js --prod",
    "build": "npm link @chassis/core && node build.js --dev"
  },
  "author": "Graham Butler",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@chassis/core": "^2.2.2",
    "productionline-web": "^1.1.8"
  }
}
