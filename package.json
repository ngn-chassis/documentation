{
  "name": "chassis-dev",
  "version": "1.0.0",
  "description": "",
  "main": "build-showroom.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "link-chassis": "cd node_modules/@chassis && npm link @chassis/core && npm link @chassis/gulp && cd ../..",
    "build-webcomponents": "build-webcomponents webcomponents/src webcomponents/dist --es5",
    "bundle-chassis-select": "bundle-webcomponent --name=chassis-select --components=chassis-select-title,chassis-options,chassis-option,chassis-optgroup,chassis-optgroup-label,chassis-select --dir=webcomponents/dist",
    "bundle-chassis-datalist": "bundle-webcomponent --name=chassis-datalist --components=chassis-options,chassis-option,chassis-datalist --dir=webcomponents/dist",
    "bundle-chassis-layout": "bundle-webcomponent --name=chassis-layout --components=chassis-layout-cell,chassis-layout --dir=webcomponents/dist",
    "bundle-webcomponents": "npm run bundle-chassis-select && npm run bundle-chassis-datalist && npm run bundle-chassis-layout",
    "build": "npm run link-chassis && npm run build-webcomponents && npm run bundle-webcomponents && gulp build",
    "build2": "build-webcomponent webcomponents/src/chassis-datalist webcomponents/dist --es5 && bundle-webcomponent --name=chassis-datalist --components=chassis-options,chassis-option,chassis-datalist --dir=webcomponents/dist && gulp build"
  },
  "author": "Graham Butler",
  "license": "BSD-3-Clause",
  "dependencies": {
    "@chassis/core": "^1.0.1",
    "@chassis/gulp": "^1.0.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-minify": "^0.2.0",
    "babel-runtime": "^6.26.0",
    "fs-extra": "^4.0.2",
    "gulp": "^3.9.1",
    "gulp-sourcemaps": "^2.6.1",
    "postcss-selector-not": "^3.0.1"
  },
  "devDependencies": {
    "babel-plugin-transform-runtime": "^6.23.0",
    "productionline": "^1.0.7",
    "shortbus": "^1.2.8"
  }
}
